version: 2

models:
    - name: test_table
      description: This is a test table
      columns:
        - name: the_float
          data_type: number
          meta: 
            masking_policy: dbt_analytics
            other_stuff: things    
        - name: the_varchar
          data_type: string
          meta:
            masking_policy: pempey
        - name: the_int
          meta:
            masking_policy: dbt_analytics
        - name: the_array
          meta:
            masking_policy: dbt_analytics
        - name: the_variant
        - name: the_date
          meta:
            masking_policy: pempey
        - name: the_timestamp
          meta:
            masking_policy: analyst_people
        - name: the_boolean
          meta:
            masking_policy: engineer